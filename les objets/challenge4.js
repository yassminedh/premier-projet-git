const prompt = require("prompt-sync")();
const point = {
    x : prompt("entrer un valeur : "),
    y : prompt("entrer un valeur : "),
};
point.x = prompt("entrer un valeur : ");
point.y = prompt("entrer un valeur : ");
console.log(point.x);
console.log(point.y);